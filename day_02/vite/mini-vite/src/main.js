// esbuild  -> go -> 打包贼快
// esbuild  -> 一个文件  -> 只返回一个文件  -> 浏览器就能理解了

// loadsh-es   ->  n个import  -> http   -> 多了浏览器吃不消
// loadsh-es  ->  n个  ->  变成1个   ->  http   ->  1个


import { createApp } from "vue";

console.log(createApp)
console.log(123)
